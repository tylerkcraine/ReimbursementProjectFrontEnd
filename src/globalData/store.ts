import { globalData } from "../interfaces/globalInterface";

export const store:globalData = {
    jwt: "",
    base_url: "http://localhost:8080",
    toast_options: {style: {
                        borderRadius: '10px',
                        background: '#333',
                        color: '#fff',
                    },}
}

